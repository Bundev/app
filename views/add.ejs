<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>CRM</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    
  </head>
  <body>
    <%- include('blocks/header') -%>
    <div class="container-fluid" id="cont">
      <div class="row">
        <div class="col-2">
          <%- include('blocks/menu') -%>
        </div>
        <div class="col-10" >
          <div class="searchbox">
            <div class="input-group">
              <input class="form-control" id="search" type="text" placeholder="Введите название товара" aria-label="default input example">
            </div>
            <div id="producttop">
              <div class="nomin"><b>Номенклатура</b></div>
              <div class="price"><b>Цена</b></div>
              <div class="stock"><b>Остатки</b></div>
            </div>
            <div id="results">
              <!-- <div class="product">
                <div class="nomin">Клапан нижнего подвода воды 1/2" пласт SOLOPLAST АНн-02 (24537), шт</div>
                <div class="price">Цена: 160</div>
                <div class="stock">Остатки: 2</div>
              </div> -->
            </div>
          </div>
          <!-- <div class="table-container">
            <table class="table">
              <thead>
                <tr>
                  <th scope="col">#</th>
                  <th scope="col">Наминклатура</th>
                  <th scope="col">Кол-во</th>
                  <th scope="col">Ед.и</th>
                  <th scope="col">Цена</th>
                  <th scope="col">Сумма</th>
                </tr>
              </thead>
              <tbody id="item-products">
                
              </tbody>
            </table>
          </div> -->
          <script>
            const itemProducts = document.getElementById('item-products');
            let html = '';
            for (let i = 0; i < 5; i++) {
              html += `
              <tr>
                <th scope="row">${i}</th>
                <td>Прокладка для арматуры Siamp ЭКО Силикон ЗАВОД (2 КНОПКИ)</td>
                <td>
                  <input class="form-control" size="4" type="text" value="2">
                </td>
                <td>Шт</td>
                <td>500</td>
                <td>1000</td>
              </tr>
            `;
              
            }
             


            itemProducts.innerHTML = html;
          </script> 
          
        </div>
        <!-- <div class="container footer-product">
            <div class="row">
              <div class="col-8">

              </div>
              <div class="col-2"><button type="button" class="btn btn-primary">Сдача</button></div>
              <div class="col-2"><div class="summa">Сумма: 500</div></div>
            </div>
            
          </div> -->
      </div>
    </div>
    <%- include('blocks/footer') -%>
<script>
  const searchInput = document.getElementById('search');
  const resultsDiv = document.getElementById('results');
  const productTop = document.getElementById('producttop');

  let timer;

  searchInput.addEventListener('input', () => {
    clearTimeout(timer);

    timer = setTimeout(async () => {
      const q = searchInput.value;

      if (q.length < 2) {
        resultsDiv.innerHTML = '';
        productTop.style.display = 'none';
        resultsDiv.style.display = 'none';
        return;
      }

      const response = await fetch(
        `/search?q=${encodeURIComponent(q)}`
      );

      const products = await response.json();
      
      resultsDiv.innerHTML = products.map(product => `
        <div class="product">
          <div class="nomin" data-name='${product.name}' data-price="${product.price}">${product.name}</div>
          <div class="price">${product.price}</div>
          <div class="stock">${product.stock}</div>
        </div>
      `).join('');
      resultsDiv.style.display = 'block';
      productTop.style.display = 'block';
    }, 300);
  });

  resultsDiv.addEventListener('click', (e) => {
  const product = e.target.closest('.nomin');

  if (!product) return;

  const text = product.dataset.name;

  const textarea = document.createElement('textarea');
  textarea.value = text;

  document.body.appendChild(textarea);
  textarea.select();

  document.execCommand('copy');

  document.body.removeChild(textarea);

  // console.log('Скопировано:', text);
});
</script>
  </body>
</html>